@model InfoViewModel

<section class="info-cards-section">
    <div class="container">
        <div class="info-cards">
            @foreach (var item in Model.InfoCards)
            {
                <partial name="/Views/Partials/_InfoCard.cshtml" model="item" />
            }
        </div>
    </div>
</section>